
proc test: num(a: num, b: num) {
    # A test function

    number: num = 2;
    say number;

    b = 10 * a;
    c: num = a - - b;

    return c;
};

x: num = 11;
b: bool = test(2, x) + 2 > 23;
say b; # Should be true
