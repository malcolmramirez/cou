
# Will be tested when call stack + functions are implemented

main() = {

    test(a, b) = {
        number = 2;
        a = number;
        b = 10 * a + 10 * ~(number / 4);
        c = a - - b;

        => c
    };

    x = 11;
    x = test(x, 2);

};
