
# Will be tested when call stack + functions are implemented

main: nada() {

    test: int(a, b) {
        number: int = 2;

        a: int = number;
        b: int = 10 * a + 10 * ~(number / 4);
        c: int = a - - b;

        => c
    }

    x = 11;
    x = test(x, 2);
}
